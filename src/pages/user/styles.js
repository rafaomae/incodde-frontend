import { StyleSheet } from "aphrodite/no-important";

export default StyleSheet.create({
  content: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    paddingTop: 30,
  },
});
